import { Overrides } from '@material-ui/core/styles/overrides'

const formControl: Overrides['MuiFormControl'] = {
  marginNormal: {
    marginTop: 0,
    marginBottom: '20px',
  },
}

export default formControl
