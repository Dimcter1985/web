export enum SortDirections {
  ASC = 'asc',
  DESC = 'desc'
}

export enum AppointmentSortBy {
  STARTS_AT = 'starts_at',
  TOTAL_AMOUNT = 'total_amount',
}

export enum SalonsSortBy {
  DISTANCE = 'distance',
  RATING = 'rating',
  FEATURED = 'featured',
  BOOKED_COUNT = 'booked_count',
}

export enum FreeSlotsSortBy {
  COST = 'cost',
  DURATION = 'duration',
  DISTANCE = 'distance',
  RATING = 'rating',
}