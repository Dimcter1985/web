import Divider from 'components/Divider'

import styles from './separator.module.scss'

const Separator: React.FC = () => {
  return (
    <Divider className={styles.item} />
  )
}

export default Separator
